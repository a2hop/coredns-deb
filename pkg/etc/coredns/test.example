# CoreDNS Test Configuration
# This is a simple configuration for testing CoreDNS
#
# To use this, copy it to the Corefile:
#   sudo cp /etc/coredns/test.example /etc/coredns/Corefile
#   sudo systemctl restart coredns
#
# Then test with:
#   dig @127.0.0.1 test.local
#   dig @127.0.0.1 google.com

# Local test zone
test.local {
    file /etc/coredns/test.local.zone
    log
    errors
}

# Forward everything else to Google DNS
. {
    forward . 8.8.8.8 8.8.4.4
    cache 30
    log
    errors
}
