# Serve local domain and proxy everything else
example.local {
    # Serve local zone from file
    file /etc/coredns/example.local.zone
    log
    errors
}

# Forward everything else
. {
    forward . 8.8.8.8 8.8.4.4
    cache 30
    log
    errors
}
